<!DOCTYPE html>
<html lang="en">
<header>
    <% include ./partials/_header %>
</header>
<head>
  <title>TinyApp</title>
</head>
<body>
    <ul>
      <% for (thing in urls[login]) { %>
      <li> <%= thing %> ---> <a href = '<%= urls[login][thing] %>'> <%= urls[login][thing] %> </a> &emsp; <a href = /urls/<%= thing %>> Edit </a> &emsp;  <form method="POST" action= '/urls/<%= thing %>/delete'>
          <input type="submit" value="Delete">
        </form></li>

      <% }; %>
    </ul>

</body>
</html>